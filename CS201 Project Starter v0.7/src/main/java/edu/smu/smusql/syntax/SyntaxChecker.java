package edu.smu.smusql.syntax;

public class SyntaxChecker {

    // Private constructor to prevent instantiation since this is a utility class
    private SyntaxChecker() {}

    // CREATE TABLE student (id, name, age, gpa, deans_list)
    public static boolean isValidInsert(String[] tokens) {
        return false;
    }

    // Helper function to check CREATE command syntax
    public static boolean checkCreateSyntax(String[] tokens) {
        return false;
    }
}